<?xml version="1.0" encoding="utf-8"?>
<!--
  androidmcp's accessibility service. It never reacts to events (notificationTimeout is moot; the
  server polls the node tree on demand); it exists only to (a) retrieve window content for
  read_screen and (b) perform global actions and dispatch gestures / text on request. Every such use
  is additionally gated by the app's per-call approval, so the grant alone reads/does nothing.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/a11y_description" />
